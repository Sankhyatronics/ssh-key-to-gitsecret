{
  "name": "ssh-key-secret",
  "version": "1.0.0",
  "description": "GitHub action for creating SSH Key. It will push private key in Secrets and display the public key in logs.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js"
  },
  "keywords": [],
  "author": "Ajit",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "tweetsodium": "0.0.6"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
